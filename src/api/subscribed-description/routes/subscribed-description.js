'use strict';

/**
 * subscribed-description router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscribed-description.subscribed-description');
