'use strict';

/**
 * subscribed-description service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscribed-description.subscribed-description');
