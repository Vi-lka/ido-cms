'use strict';

/**
 * suggest-description router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::suggest-description.suggest-description');
